# M1.16 赣鄱高岭矿丘与河运柴场人工地图记录

日期：2026-06-15

## 目标

本切片关闭赣鄱章节 `chapter-ganpo-kiln-firewood` 的直接人工地图缺口：`ganpo-kaolin-hill` 与 `ganpo-river-wood`。本轮只冻结程序入口、交互目标、守卫矩阵与验证口径，不处理最终美术资源、装饰密度和精细坐标摆位。

## 六团队审计结论

M1 固定 6 团队已按程序极速协作指引执行只读审计：

| 团队 | 范围 | 结论 |
| --- | --- | --- |
| A | 范围与验收 | 两张人工 JSON、运行时注册、关键入口矩阵与赣鄱 layout gap 收束为最小验收。 |
| B | 内容与 ID | 高岭矿丘落 `harvest-kaolin`、`mine-kaolin`、`gp-kaolin-hill`、`gp-shi-bai`；河运柴场落 `harvest-coal`、`xiabu`、`gp-river-wood-yard`、`gp-chai-yazi`。 |
| C | 地图与 gate | 沿用 56x30、三横三竖路网、中心出生点；区内门互通，跨区门保留江南、徽州、荆楚。 |
| D | 活动链路 | `gp-river-wood-yard` 已有 route challenge；`gp-kaolin-hill` 可走无 challenge 的直接活动完成链。 |
| E | QA | 地图/入口/冻结/章节审计为最小聚焦守卫；canvas gate 移动不稳定时以 Vitest 可达性与内置浏览器可见流程互补。 |
| F | 文档 | 新增本记录，并同步 M1 总表、程序收束记录、ID 冻结规范与 M0.9 历史快照追记。 |

## 程序落点

- 新增 `src/data/mapLayouts/ganpo-kaolin-hill.json`。
- 新增 `src/data/mapLayouts/ganpo-river-wood.json`。
- `src/data/mapLayout.ts` 已导入两张 JSON 并加入 `RUNTIME_MAP_EDITOR_SNAPSHOTS`。
- `ganpo-kiln-town` 原有去两地的 `subregionGate` 保持不变；两张新图均补回主镇和彼此互通门。
- 赣鄱跨区门继续使用普通 `gate`，目标为 `jiangnan`、`huizhou`、`jingchu`，不新增路线 ID，不改变正式迁移必须经街景 gate 的红线。

## 交互点

`ganpo-kaolin-hill`：

- 产业：`harvest-kaolin`、`mine-kaolin`
- 活动：`gp-kaolin-hill`
- NPC：`gp-shi-bai`
- 区内 gate：`ganpo-kiln-town`、`ganpo-river-wood`
- 跨区 gate：`jiangnan`、`huizhou`、`jingchu`

`ganpo-river-wood`：

- 产业：`harvest-coal`
- 工艺入口：`xiabu`
- 活动：`gp-river-wood-yard`
- NPC：`gp-chai-yazi`
- 区内 gate：`ganpo-kiln-town`、`ganpo-kaolin-hill`
- 跨区 gate：`jiangnan`、`huizhou`、`jingchu`

## 守卫同步

- `mapLayout.test.ts` 固定 shipped layout 清单并断言两张新图关键对象。
- `priorityMapLayoutEntrypoints.test.ts` 将两张新图纳入当前优先入口矩阵。
- `priorityFrozenIds.test.ts` 冻结两张 layout 与本轮实际落图 NPC：`gp-shi-bai`、`gp-chai-yazi`。
- `regionChapterAudit.test.ts` 不再期待赣鄱两张 layout gap。
- `fullScopeAudit.test.ts` 不再期待赣鄱 `layout-subregion:1/3`，但保留 `xiabu` 缺专属工艺交互的后续深度缺口。

## 验证

已通过聚焦验证：

```bash
npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/engine/__tests__/fullScopeAudit.test.ts
```

结果：5 个测试文件、49 项用例通过。

已通过全量验证：

```bash
npm test
npm run typecheck
npm run build
```

结果：`npm test` 35 个测试文件、445 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。

已使用内置浏览器验证：

- `?chapterSmoke=chapter-ganpo-kiln-firewood&smokeSubregion=ganpo-kaolin-hill&qaSeason=autumn&qaWeather=clear`
- `?chapterSmoke=chapter-ganpo-kiln-firewood&smokeSubregion=ganpo-river-wood&qaSeason=autumn&qaWeather=clear`

结果：两张地图均可从 DEV-only 起点载入，画布存在，HUD 显示对应小地区；高岭矿丘活动可打开并完成；河运柴场挑战可选择“按货重、人情与天气重排路线”后完成；控制台 error/warn 为空。正式 gate 可达性仍由 `priorityMapLayoutEntrypoints.test.ts`、`mapLayout.test.ts` 与章节导航守卫覆盖，不使用总览地图迁移替代。

## 未关闭项

- `xiabu` 仍缺完整专属工艺交互、缺陷、返修与订单反馈链。
- 画坯坊/釉料来源对作品诊断的读取仍待扩展。
- 窑柴风险、瓷行藏客与多入口 `smokeBindings` 仍属后续内容密度任务。
- 最终地表瓦片、建筑、装饰、NPC 站姿与精细摆位不在本切片完成范围。
