# M1.13 雪域雪山驿口人工地图记录

日期：2026-06-15

2026-06-15 追记：M1.14 已继续补齐 `xueyu-pigment-valley` 与 `xueyu-silver-tent` 人工地图。本文件的“剩余缺口”记录为 M1.13 当时快照；当前雪域直接核心人工地图缺口已关闭，后续转向人物回访、材料状态反馈和护商后日谈。

## 节点口径

本轮继续按 [程序大型化协作指引.md](程序大型化协作指引.md) 的 M1 口径推进，固定启用 6 个协作团队做只读审计。切片目标是关闭雪域章节中最直接的程序断点：`xueyu-snow-pass` 已经是章节 smoke、路线落点、活动订单和牦牛队长 NPC 的正式目标，但运行时仍缺人工地图 JSON。

本轮不扩成完整雪域大章，不新增 NPC，不改路线落点；只让雪山驿口成为可从街景进入、可交互、可经 gate 往返的正式运行地图。颜料矿谷、银器帐房人物回访和雪山驿路后日谈继续保留为后续 M1/M3 内容。

## 本轮完成

1. 新增 `src/data/mapLayouts/xueyu-snow-pass.json`
   - 雪山驿口正式进入运行时人工地图管线。
   - 点位包含 `xy-snow-pass` 活动与 `xy-yak-captain` NPC。
   - 区内通道连接 `xueyu-thangka-court`、`xueyu-pigment-valley`、`xueyu-silver-tent`。
   - 跨区 gate 保持巴蜀雪口路与西域驼队路，不新增总览地图直跳。

2. 更新 `src/data/mapLayout.ts`
   - 注册 `xueyuSnowPassMap` 到 `RUNTIME_MAP_EDITOR_SNAPSHOTS`。

3. 更新地图与冻结守卫
   - `mapLayout.test.ts` 要求雪山驿口 JSON 含活动、NPC 与返回唐卡画院的区内通道。
   - `priorityMapLayoutEntrypoints.test.ts` 将 `xueyu-snow-pass` 纳入 M1 附加 shipped layout 矩阵。
   - `priorityFrozenIds.test.ts` 将 `xueyu-snow-pass` 纳入冻结 layout。

4. 更新章节契约
   - `chapter-xueyu-thangka-snowpass` 的 gap 不再记录 `xueyu-snow-pass` 缺人工 JSON。
   - 章节下一步动作改为优先内置浏览器复验雪山驿口与各 gate，后续再补颜料矿谷/银器帐房回访和雪山驿路后日谈。

## 验证

已通过：

- `npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/xueyuLongline.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`
  - 9 个测试文件、80 项用例通过。
- `npm test`
  - 35 个测试文件、435 项用例通过。
- `npm run typecheck`
  - 通过。
- `npm run build`
  - 通过，保留既有 Vite 大 chunk 提醒。
- 内置浏览器 smoke：`http://127.0.0.1:5173/?chapterSmoke=chapter-xueyu-thangka-snowpass&qaSeason=winter&qaWeather=snow`
  - HUD 进入 `雪域高原 · 雪山驿口`，小地图可见 1 个活动点、3 个区内 gate、2 个跨区 gate。
  - 街景互动打开 `雪山驿口` 活动，选择 `按货重、人情与天气重排路线` 后可完成体验并回到街景。
  - 从雪山驿口经区内 gate 进入 `雪域高原 · 唐卡画院`，再经唐卡画院 gate 返回雪山驿口。
  - 从雪山驿口经 `雪山驿路` 前往 `巴蜀 · 茶马驿`，再从茶马驿经同一路线返回 `雪域高原 · 雪山驿口`。
  - 从雪山驿口经 `高原丝路驿线` 前往 `西域 · 驼队驿站`，再从驼队驿站返回 `雪域高原 · 雪山驿口`。
  - 全程未使用总览地图迁移，控制台 error/warn 为空。

## 剩余缺口

- `xueyu-pigment-valley` 与 `xueyu-silver-tent` 仍缺人工 JSON。
- 颜料矿谷/银器帐房人物回访仍未补成长线。
- 雪山驿路护商结果尚未进一步接入供展续单后日谈。
- 本轮不处理最终美术资源、雪地瓦片和精细摆位。
