# M0.9 全地区生活规格入轨记录

日期：2026-06-14

复核日期：2026-06-16

## 节点目标

M0.9 接在当前优先版 N5 之后，用于把“当前优先纵切”转入“完整版大型生活模拟”的程序侧工作流。此节点不追求一次性补完 11 区全部内容，而是先把缺口转成可执行、可测试、可分工的矩阵，防止后续继续停留在文档愿景或零散加表。

执行依据：[程序大型化协作指引.md](程序大型化协作指引.md)。

## 协作团队

按 M0.9 固定团队数启动 5 个只读审计团队，符合指引要求。

| 团队 | 代号 | 审计范围 | 关键结论 |
| --- | --- | --- | --- |
| A 集成与数据基线 | Darwin | 状态树、数据表、测试基线、冻结 ID | N5 基线可复用；M1 需要新增 11 区章节线数据，不应硬扩当前优先版 `priorityJourney`。 |
| B 地区与 NPC 生活循环 | Hooke | 11 区生活链、人物链、活动链、订单链 | 江南、赣鄱、岭南、西域、巴蜀接近章节线；黔滇最薄，其余骨架区缺结构化章节。 |
| C 工艺与作品反馈 | Dalton | 工艺状态机、缺陷、返修、导师、订单拒收 | 阶段诊断和返修底座可复用；M2 需半实时输入 payload，M1 先预留字段和 UI 钩子。 |
| D 地图与正式移动 | Dewey | 人工地图、街景 gate、总览红线 | 正式移动红线当前守住；M1 前要补 gate 来源凭证、route 落点和 JSON 注册/语义守卫。 |
| E 经济、引导与 QA | Kepler | 长局经济、维护费、断供、教程、失败态 | 经济底座存在；长局测试还缺金币流、维护缺缴、订单利润、库存积压等量化指标。 |

2026-06-16 按 M0.9 固定团队数再次启动 5 个只读审计智能体，符合“5 团队”要求。本轮不替换原始入轨记录，只把 M1.30-M1.32 之后的真实程序状态同步回矩阵。

| 团队 | 智能体 | 复核范围 | 复核结论 |
| --- | --- | --- | --- |
| A 集成与数据基线 | Erdos | 状态树、数据出口、冻结 ID、测试基线 | `FULL_SCOPE_REGION_REQUIREMENTS` 与 `REGION_CHAPTERS` 是后续总入口；冻结 ID 守卫少覆盖 3 个已入轨小地区，本轮已补齐到 43/43。 |
| B 地区与 NPC 生活循环 | Dewey | 11 区生活链、人物链、活动链、订单链 | 当前章节审计为 5 个 `chapter-ready`、6 个 `needs-expansion`、0 个未知引用；缺口集中在非江南 `smokeBindings`、人物回访、订单差异和生活密度。 |
| C 工艺与作品反馈 | Franklin | 工艺规则、缺陷、返修、导师、订单拒收 | 规则层已能承接缺陷、返修、师傅复核与拒收；M2 关键是新增可复用半实时输入桥，而不是继续堆孤立按钮。 |
| D 地图与正式移动 | Maxwell | 43 张人工地图、街景 gate、总览红线 | 43/43 地图已入轨且红线守住；发现江南金陵 / 临安缺显式双向人工 `subregionGate`，本轮已补 JSON 和测试守卫。 |
| E 经济、引导与 QA | Sartre | 长局经济、库存、维护费、教程、浏览器 smoke | 经济底座可用但仍不是 M3 级报告；M3 前要新增 30/60/120 天 deterministic 长局风险表。 |

## 本轮程序落点

本节点先补一层“完整版范围巡检”，让 M0.9 不是纯文档结论，而能被测试读取。

新增/接入：

1. `src/data/fullScope.ts`
   - 定义 11 个完整版地区的 M1 分组、章节目标、三类玩法支柱、最低覆盖指标和 M1 动作。
   - 保留当前优先五区与骨架六区的差异，但不再把六区只视为资料卡骨架。
2. `src/engine/fullScopeAudit.ts`
   - 提供纯函数 `buildFullScopeAudit`。
   - 按地区统计小地区、NPC、活动、路线、招牌工艺、工艺交互、工坊升级、人工地图、百工志、订单钩子、联作、家访和护商事件。
   - 输出 `ready / partial / gap`，并暴露稳定缺口字符串。
   - 加严未知引用守卫：检查 `REGION_CONTENT`、`SUBREGION_CONTENT` 和工艺交互中的 NPC、活动、小地区、工艺与导师引用。
3. `src/engine/__tests__/fullScopeAudit.test.ts`
   - 守卫 11 区都进入 M0.9 审计矩阵。
   - 守卫当前优先版不能被误判为完整版完成。
   - 固化示例缺口：江南六张人工地图已由 M1.17/M1.24 关闭，赣鄱高岭矿丘/河运柴场两张人工地图已由 M1.16 关闭；西域艾德莱斯织坊和 `atlas-silk` 工艺深度已由 M1.15 收束，后续示例转向材料读数、河运风险与更长线回访。
4. `src/data/index.ts`、`src/engine/index.ts`
   - 导出完整版范围数据和审计函数，供后续 UI、测试和工具接入。

## 11 区当前判断

| 地区 | 当前判断 | M1 主要动作 |
| --- | --- | --- |
| 江南 | `chapter-ready` | M1.17/M1.24-M1.29 已补六张人工地图、临安 / 太湖回访、苏小茶纸路、蓝染 / 竹编深度、6 入口 `smokeBindings` 与灯市关系读数；本轮补金陵 / 临安双向人工 gate，程序侧仅保留最终美术摆位复验。 |
| 巴蜀 | 锚点章偏薄 | M1.20 已补临邛铁炉入口；后续补铁料订单回流、茶马会路线回访、蜀锦/青神竹编订单差异。 |
| 岭南 | 接近章节线 | M1.21 已补佛山冶坊、端石坑口两张人工地图；后续补佛山铁器修造回流、骑楼夜市船期回访和端砚/石湾陶订单读缺陷。 |
| 黔滇 | `needs-expansion` | M1.22 已补东川铜矿人工地图，M1.31 已补铜山客东川铜银料账回访；后续转向蜡染人物长线、岭南港口回访延展和多入口 `smokeBindings`。 |
| 荆楚 | 骨架偏厚 | M1.23 已补铜铁矿场与湘绣楼两张人工地图，M1.32 已关闭大冶矿料状态反馈；后续做“水路楚漆修复包”：渡口水运三阶段摊位、楚漆修复和湘绣回访订单链。 |
| 赣鄱 | 接近章节线 | M1.16 已补高岭矿丘、河运柴场两张人工地图；后续开窑会接瓷行藏客、瓷土/釉料读数和窑柴风险。 |
| 徽州 | 骨架偏厚 | 做“纸墨荐藏包”：商会荐藏、纸墨合单、江南/赣鄱跨区回流。 |
| 京畿 | 骨架偏厚 | 做“宫造采办包”：官门、景泰蓝、贡造护送、采办许可和商誉。 |
| 三晋 | `needs-expansion` | 煤铁窑塬、清徐醋坊、票号信用、复违约利钱和民生日用账线已接；后续转向 M3 级多轮本金 / 利钱 / 债务统计。 |
| 雪域 | 骨架偏厚 | 做“唐卡净室与雪口补给包”：材料状态、订单验收、高原补给联动。 |
| 西域 | `chapter-ready` | 艾德莱斯织坊入口和 `atlas-silk` 工艺已入轨；后续转向巴扎长线商路风险、织坊藏客回访、玉作 / 织坊订单差异和多入口 `smokeBindings`。 |

## P0/P1 程序动作

P0：

1. 新增 `RegionChapterSpec` 或同级结构，不直接硬扩当前优先版 `priorityJourney`。
2. 将 11 区每区至少一条核心章节线接入结构化数据，并守卫章节引用的 craft、activity、NPC、route、lore、homeVisit、collab 均存在。
3. 为正式移动补 gate 来源/近身凭证；后续验证不能只靠 reducer 直达或 `placeAt`。
4. 为跨区路线增加 `routeId -> landingSubregionId`，避免 `TRAVEL` 默认落到目标地区第一个小地区。
5. 建立 M1 级长局经济指标：金币流、维护缺缴、订单利润、库存积压、路线恢复、声望净变化。

P1：

1. 新增或拆出 `orderChains` 数据入口，避免订单链分散在活动、家访、NPC 功能和测试中。
2. 人工地图已推进到 43/43；冻结 ID 守卫已补齐 `qiandian-tea-road`、`jingchu-lake-market`、`huizhou-merchant-hall`，直接人工地图缺口清零，后续转向内容链、经济模拟和美术精修。
3. JSON 注册和 gate 语义增加守卫：新增文件必须进入运行时；gate 必须显式声明跨区或区内。2026-06-16 已补江南金陵 / 临安双向人工 `subregionGate`，避免运行时 fallback 代替人工点位。
4. 工艺规格预留半实时桥字段：来源、前置、状态变量、每阶段输入类型、失败标签、返修窗口和社会用途。
5. 教程和失败态补维护费、断供、订单过期、押金/赊账、修复和失败恢复说明。

## M1 第一批章节包

2026-06-16 复核后，M1 第一批不再优先补地图入口，而是优先补“可审计的多入口与反馈链”。

1. F3 前置：把江南 M1.28 的 `smokeBindings` 模式推广到非江南 10 区，至少覆盖入口小地区、代表活动、代表工艺、关键 NPC 与正式 route landing case。
2. 荆楚「水路楚漆修复包」：渡口水运三阶段、楚漆追单、湘绣楼人物回访和水路材料外溢。
3. 徽州「纸墨荐藏包」：商会信用风险、荐藏回访、宣纸 / 徽墨 / 歙砚订单差异。
4. 雪域「唐卡净室与雪口补给包」：颜料矿谷 / 银器帐房人物回访、材料状态反馈和雪山驿路后日谈。
5. 黔滇「银染茶马礼俗包」：丹青嫂蜡染长线、岭南港口回访延展和本地蜡染语境。
6. Ready 区补密度：巴蜀补临邛铁料订单回流、龙泉剑材料读数和茶马会后日谈；岭南补佛山铁器修造回流、端砚 / 石湾陶缺陷读取和骑楼船期后日谈；赣鄱补瓷土 / 釉料来源读数和河运长线风险；西域补巴扎商路风险、织坊藏客回访和玉作 / 织坊订单差异。

## M2/M3 前置记录

M2 工艺样板优先级：

1. 龙泉宝剑：火候、锤击、淬火、研磨。
2. 宣纸：水感、浆浓、荡帘、焙墙湿度。
3. 玉雕：相玉、开料、顺绺、抛光与不可逆取舍。

M2 输入桥设计口径：

- 新增可复用 `CraftInputBridgeSpec` 时，应挂接阶段、变量轴、目标区间、容差、失败标签、返修窗口、导师提示和订单用途。
- 实时或半实时小游戏只产出压缩摘要，例如 `stageId`、`controlScore`、`riskScore`、`dimensionDelta`、`tags`、`attempts`；reducer 继续保持纯函数结算，不保存高频原始采样。
- 旧按钮模型可以映射为合成输入摘要，保证旧档和已有工艺页不被 M2 桥接打断。

M3 经济指标必须从 M1 起就埋点或测试：

- 每 30/60/120 天金币 min/max/end、净现金流、破产或卡死天数。
- 工坊维护应缴/实缴/缺缴比例，连续缺缴次数和指标下滑。
- 订单接取/完成/过期/违约数量，平均利润，押金损失，品相失败和库存不足。
- 资源峰值、死库存、周转率、售卖/订单/修复消耗占比。
- 路线安全/紧张/危险天数，断供生成/解决/接受短缺和恢复时间。
- 每区声望 gain/loss，断供和订单对声望的净影响。

## 验证

已通过：

- `npm test -- --run src/engine/__tests__/fullScopeAudit.test.ts src/engine/__tests__/priorityScopeAudit.test.ts`：2 个测试文件、6 项用例通过。
- `npm test -- --run src/engine/__tests__/fullScopeAudit.test.ts`：1 个测试文件、3 项用例通过。
- `npm test`：25 个测试文件、312 项用例通过。
- `npm run build`：通过，保留既有 Vite 大 chunk 提醒。
- 2026-06-16 M0.9 五团队复核聚焦验证：`npm test -- --run src/engine/__tests__/fullScopeAudit.test.ts src/data/__tests__/mapLayout.test.ts src/game/__tests__/regionSpec.test.ts src/data/__tests__/priorityFrozenIds.test.ts`：4 个测试文件、34 项用例通过。
- 2026-06-16 M0.9 五团队复核全量验证：`npm run typecheck` 通过；`npm test` 37 个测试文件、507 项用例通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。

## 当前结论

M0.9 已完成“入轨”动作，并在 2026-06-16 完成五团队复核：后续不再凭口头判断推进完整版，而是先以 `FULL_SCOPE_REGION_REQUIREMENTS`、`REGION_CHAPTERS`、`buildFullScopeAudit` 和 `buildRegionChapterAudit` 做程序矩阵。

2026-06-16 M1.33 已关闭非江南 `smokeBindings` 推广：全地区 43/43 章节入口小地区已纳入 `REGION_CHAPTERS.smokeBindings`，`buildRegionChapterAudit.smokeBindingCoverageGaps` 可直接暴露覆盖缺口。M1 下一批优先推进 needs 区反馈链、ready 区密度补线、M2 半实时工艺桥预案和 M3 长局经济报告；美术资源替换仍不阻塞程序侧继续推进。
