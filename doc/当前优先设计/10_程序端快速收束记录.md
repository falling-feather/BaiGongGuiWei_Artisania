# 程序端快速收束记录

更新时间：2026-06-15

## 本轮目标

文档设计接近完成后，程序侧优先把“当前优先版主轴”落为可运行、可测试、可在 HUD 中看到的目标链，保证后续美术接入时不会只剩散落系统而缺少玩家下一步指引。

## 已实装

1. 新增 `src/data/priorityJourney.ts`
   - 定义五个优先锚点地区的主轴步骤：江南、巴蜀、岭南、赣鄱、西域。
   - 江南、巴蜀、岭南、赣鄱拆为两个里程碑：代表工艺样品完成、代表节会/摊位链闭环。
   - 西域拆为三个里程碑：玉作样品、绿洲巴扎闭环、驼队驿站远行确认。
   - 所有里程碑绑定真实工艺 ID、活动 ID、完成 flag 与百科目标词条。

2. 新增 `src/engine/priorityJourney.ts`
   - 提供纯函数 `buildPriorityJourneyGuide`。
   - 根据 `GameState` 自动判断当前未完成 step/milestone。
   - 复用现有百科行脚路线引导，只提示“怎么到目标附近”，不提供地图直接迁移。

3. 接入 `src/components/Hud.tsx`
   - HUD 常驻显示“主轴目标 x/5”。
   - 玩家主动追踪百科词条时，原有“行脚目标”仍然保留。
   - 主轴目标和百科追踪互不抢占，便于后续调试与正式游玩并行。

4. 补充 `src/index.css`
   - 新增 `.hud--priority-guide`。
   - 位置高于原有行脚目标，避免与控制提示和百科追踪重叠。

5. 补充自动化守卫
   - `src/engine/__tests__/priorityJourney.test.ts`
   - `src/data/__tests__/priorityScope.test.ts`
   - 覆盖主轴目标推进、里程碑完成条件、数据 ID 断链检查。

6. 新增当前优先版程序巡检
   - `src/engine/priorityScopeAudit.ts`
   - `src/engine/__tests__/priorityScopeAudit.test.ts`
   - 把主轴五区里程碑、主轴资料齐备度和骨架六区资料齐备度输出为可测试结构。

7. 接入镇务收束入口
   - `src/components/RegionPanel.tsx` 新增“当前优先版收束巡检”与“本地工坊”。
   - 本地工坊只开放当前小地区已有工艺，不触发迁移，不替代街景 gate。
   - `coin`、`labor` 在镇务和工艺页展示为“通货”“工时”。

8. 新增主轴活动街景入口守卫
   - `src/game/__tests__/priorityActivityEntrypoints.test.ts` 覆盖江南、巴蜀、岭南、赣鄱、西域五个主轴活动。
   - 守卫目标小地区必须暴露对应活动点。
   - 守卫活动里程碑的导航只能指向真实 `subregionGate`，不新增总览地图直跳。

9. 补齐活动收束入口互锁
   - `src/components/ActivityModal.tsx` 在摊位收束未决时禁用“完成体验”。
   - `src/components/RegionPanel.tsx` 在同一状态下禁用镇务本地活动入口。
   - `src/game/__tests__/activityUiEntrypoints.test.ts` 固化 UI 入口与 reducer 规则一致。

10. N1 第一批主轴程序补线
   - 新增主轴活动目标小地区运行地图：金陵、锦里、茶马驿、珠江商港、绿洲巴扎、驼队驿站。
   - 扩展 `PRIORITY_SCOPE_REQUIREMENTS`，主轴五区不再只要求首批入口图，而是要求主轴实际使用小地区均有 shipped layout。
   - 新增 `interaction-qingshen-bamboo`，补齐青神竹编三阶段工艺模拟、缺陷和返修。
   - 新增集中冻结 ID 守卫和主轴点位守卫，防止命名、地图入口和活动链回退。

11. N1 浏览器深烟测收口
   - 新增开发态 `?smoke=` 内存场景，覆盖江南、巴蜀、岭南、赣鄱、西域巴扎和西域驼队驿站。
   - `smokeMode=true` 时 reducer 派发不写入玩家存档槽，便于反复复测主轴场景。
   - 补齐活动、策略、收束选择和镇务入口的 UI smoke 选择器。
   - 修复活动弹窗高度与滚动，保证三阶段节会在浏览器视口内可完成。

12. N2 骨架六区不断链收口
   - 按 N2 口径启用 2 个骨架团队 + 1 个 QA 团队审计六区。
   - 扩展开发态 `?smoke=` 内存场景，覆盖黔滇、荆楚、徽州、京畿、三晋、雪域。
   - 新增六区本地工艺、活动、NPC 反馈、任务/路线入口 reducer 矩阵。
   - 新增骨架入口小地区到 scoped activity 的 layout 守卫。
   - 清理三晋测试夹具旧 ID，统一到冻结 ID `sanjin` / `sanjin-lacquer-yard`。

13. N3 工艺与反馈收束
   - 按 N3 口径启用 2 个工艺团队 + 1 个 QA 团队审计阶段诊断、师傅反馈、返修、订单拒收和失败路径。
   - 普通工坊 `TAKE_ORDER` 改为读取作品实例的有效品相和重缺陷，避免绕过返修/拒收系统。
   - `CraftPage` 新增工艺诊断、材料注意、订单钩子、缺陷返修预案和最近出品病根显示。
   - `NpcDialogModal` 将订单拒收原因直接显示在订单卡片中，不再只依赖按钮 title。
   - 新增工时不足、原料不足、青神竹编市场拒收、玉作师傅返修、唐卡信用单拒收/交付的集中守卫。

14. N4 地图与移动收束
   - 按 N4 口径启用地图团队、UI 红线团队和 QA 团队审计街景入口、通道、总览地图和浏览器 smoke 缺口。
   - 当前优先人工街景从 17 张扩到 22 张，新增黔滇茶马驿道、荆楚渡口市集、徽州徽商会馆、京畿官署门房、三晋日升票号。
   - 补齐龙泉山坊、金陵、锦里、珠江商港的 live 本地点位缺口，减少 fallback 自动排布。
   - 总览地图、资料卡和普通组件继续不得派发正式迁移；`?editor=1` 收紧为开发态入口。
   - 新增 22 图点位矩阵、组件迁移红线扫描和交互点可达性守卫。
   - 内置浏览器已补 `?smoke=jiangnan` 总览地图负向 smoke，并抽查 `?smoke=qiandian`、`?smoke=jingji` 的 HUD/小地图点位与控制台状态。
15. N5 交付前压力测试
   - 按 N5 口径启用路线/存档、活动订单、工艺 NPC、结局经济 4 个审计团队回头检查。
   - 新增 `priorityPreShipStress.test.ts`，集中覆盖新档主轴、继续档跨区、五区活动订单、缺陷返修、11 区结局和长局经济压力。
   - 主轴活动里程碑从 `stall-chain-completed:*` 收紧为 `stall-closing-resolved:*`，避免摆摊三阶段完成但收束选择未处理时提前推进。
   - 结局报告地区回声不再只截取前三名，当前优先 11 区可在同一局报告中全部读取。
   - 内置浏览器复验江南活动、雪域唐卡拒收和西域驼队路线，控制台无 error。
16. M0.9 完整版程序入轨
   - 按 `doc/完整版设计/程序大型化协作指引.md` 启用 5 个只读审计团队，覆盖集成基线、地区 NPC、工艺反馈、地图移动、经济 QA。
   - 新增 `src/data/fullScope.ts` 与 `src/engine/fullScopeAudit.ts`，把 11 区完整版章节目标、玩法支柱、最低覆盖指标和缺口输出为可测试矩阵。
   - 新增 `src/engine/__tests__/fullScopeAudit.test.ts`，守卫 M0.9 审计覆盖 11 区，并防止当前优先版被误判为完整版完成。
   - 新增 `doc/完整版设计/M0_9全地区生活规格入轨记录.md`，固化 5 队审计结论、M1 第一批章节包和 M2/M3 前置指标。
17. M1 全地区核心章节入库第一切片
   - 按 M1 口径启用 6 个只读审计团队，覆盖集成结构、地区章节、QA 与街景导航。
   - 新增 `src/data/regionChapters.ts`，把 11 区核心章节、三类玩法、三类人物、订单钩子、smoke 场景和缺口入库。
   - 新增 `src/engine/regionChapterAudit.ts`，把章节未知引用、缺人工地图、proposed 钩子输出为可测试审计结果。
   - 新增 `regionChapters`、`regionChapterMapEntrypoints`、`regionChapterAudit` 三组守卫，防止章节表成为孤立 Markdown 或孤立数据。
   - 新增 `doc/完整版设计/M1全地区核心章节入库记录.md`，记录本切片完成项与 M1 后续动作。
18. M1 路线落点与章节导航守卫
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖路线落点、章节导航、地图 gate、数据契约、文档和 smoke 风险。
   - `RouteSpec` 新增双向 `landingSubregionIds`，正式街景 `TRAVEL(routeId)` 会落到路线声明的小地区。
   - 街景 gate 事件携带 `routeId`，React 只通过当前街景 guard 校验后的 gate 派发正式迁移。
   - `regionChapterAudit` 新增 `routeLandingGaps`，章节路线缺落点或落点漂移会被审计为 invalid。
   - 新增 `regionChapterNavigation.test.ts`，用 24 条章节路线用例证明章节落点经街景 gate 可达，而不是 reducer 直达。
19. M1 独立章节 Smoke 入口
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖 smoke registry、地区入口、store/App、测试、文档和浏览器风险。
   - 新增 `src/dev/regionChapterSmokeScenarios.ts`，11 区章节各有独立 smoke ID 与 DEV 内存状态构造器。
   - `REGION_CHAPTERS.smokeScenarioIds` 从当前优先版 priority smoke ID 切换为独立章节 smoke ID。
   - `App`/`gameStore` 新增 `?chapterSmoke=<scenarioId>` 入口，旧 `?smoke=` 保持兼容，章节 smoke 优先。
   - 新增 `regionChapterSmokeScenarios.test.ts`，守卫章节 smoke 覆盖、真实引用、工艺/活动/NPC reducer 链和 route landing gate。
20. M1 真实订单链补线
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖活动订单结构、成熟样板、六区缺口和 QA 矩阵。
   - 黔滇、荆楚、徽州、京畿、三晋、雪域六个 `needs-expansion` 章节入口活动新增 `generatedOrder`。
   - `REGION_CHAPTERS.orderHooks` 同步标记六个活动 hook `readsItemState: true`，黔滇移除 proposed 订单 hook。
   - 新增 `regionChapterOrderChains.test.ts`，守卫章节入口活动能生成真实订单，低品相同资源作品被拒收，高品相作品可交付并写入活动/路线订单完成 flags。
   - 章节状态仍保留 `needs-expansion`，后续转向家访、联作、节事摊位、信用/声望长线、人工地图 JSON 和多入口 smokeBindings。
21. M1 黔滇礼银长线收束
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖黔滇长线结构、成熟样板、后续优先级和 QA 文档矩阵。
   - `qd-tea-horse-road` 新增 `银染茶马会货摊` 三阶段摊位，包含 3 类客群、3 个组合、3 条策略和 3 个收束后续单。
   - `qd-yinniang-alan` 新增礼银家访二跳，能从 `silver-ritual-ledger` 复单交付推进到 `silver-ritual-renewal-ledger` 续订单。
   - 新增 `collab-alan-silver-ritual-fit`，让苗银/乌铜走银/藏银/花丝饰品可通过阿岚联作接上蜡染礼衬或茶马路牌。
   - 新增 `qiandianLongline.test.ts`，守卫章节绑定、摊位收束追单、家访复单回访、联作伙伴记忆，并把新增长线 ID 纳入冻结守卫。
22. M1 京畿声望采办门槛
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖京畿采办、三晋信用、雪域供展、成熟样板和 QA 文档矩阵。
   - `jj-song-yasi` 的 NPC 订单新增 `palace` 采办条款，读取地区声望、NPC 好感、商贸/人情属性、路线风险和宫造背书。
   - 低商誉生成 `官样采办预审单` 并收名帖保金；高商誉或宫造背书生成 `官样采办许可单` 并免押。
   - 正式采办单继续走 `palace` 清洁作品验收，交付后写入 `palace-order-completed:jj-song-yasi` 并抬升京畿地区声望。
   - 新增 `jingjiProcurement.test.ts`，守卫章节绑定、预审押金、正式许可、商誉抬升和 tracked work 交付。
23. M1 雪域供展回访切片
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖雪域供展、三晋对照、成熟样板、地图移动和 QA 文档矩阵。
   - `homevisit-losang-patron-return` 改为同时要求 `homevisit-referral-completed:reverent-loan-hall` 与 `activity-order-completed:xy-snow-pass`。
   - `reverent-loan-hall` 的净室供展复单固定为唐卡 1 件、最低品相 0.64，避免随陈列品质量漂移。
   - 新增 `xueyuLongline.test.ts`，守卫雪口补给单、净室复单、藏客回访、净室续单和续单交付。
   - 雪域仍保留 `needs-expansion`，人工 JSON、颜料矿谷/银器帐房回访与雪山驿路后日谈后续推进。
24. M1 三晋信用违约回本修复切片
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖三晋信用、订单经济、成熟样板、地图移动红线和 QA 文档矩阵。
   - 雷掌柜信用单逾期后，后续新单读取 `credit-default:sj-lei-zhanggui` 并降低信用条款；完成票号漆档续订单后写入 `sanjin-credit-payback-restored` 等修复 flags。
   - 新增 `sanjinCreditRecovery.test.ts`，守卫违约扣声望、押金恶化、票号漆档续单回本修复、修复后信用恢复与无失约负例。
   - 三晋仍保留 `needs-expansion`，后续转向复违约利钱后日谈、更多账本分支、煤铁窑塬/清徐醋坊人工 JSON 和人物回访。
25. M1 三晋复违约利钱后日谈与账本分支
   - 按 M1 口径继续启用 6 个只读审计团队，覆盖集成结构、成熟样板、商贸链、文房样板、三晋专项和 QA 浏览器矩阵。
   - 雷掌柜信用单在旧失约修复后再次逾期，会写入复违约、利钱挂账和账本后日谈 flags；后续新单文案显示“复违约利钱入账”。
   - 新增 `homevisit-lei-credit-ledger-aftertalk`，支持利钱留凭直接结清、补账续单结清和煤铁/醋坊账线线索，且补账续单打开后不会重复刷单。
   - 三晋仍保留 `needs-expansion`，后续转向煤铁/醋坊账线实化、煤铁窑塬/清徐醋坊人工 JSON、人物回访和 M3 多轮经济。

## 当前验证

已通过：

- `npm run typecheck`
- `npm test -- priorityJourney priorityScope`
- `npm test -- priorityScopeAudit priorityScope priorityNewGameCoreLoop`
- `npm test -- priorityActivityEntrypoints priorityJourneyNavigation regionSpec`
- `npm test -- activityUiEntrypoints reducer`
- `npm test -- priorityJourney priorityJourneyFlow mapLayout priorityMapLayoutEntrypoints priorityFrozenIds priorityScope`：8 个测试文件、36 项用例通过。
- `npm test`：24 个测试文件、309 项用例通过。
- `npm run build`：通过，保留既有 Vite 大 chunk 提醒。
- 浏览器新档深烟测：江南苏杭水市经街景通道到龙泉山坊，采集、冶铁、制作龙泉宝剑、接订单售卖，控制台无 error/warn。
- 浏览器本地活动短烟测：`localhost:5173` 继续烟测档打开镇务与 `龙泉剑炉` ActivityModal，验证挑战选择前后“完成体验”禁用/解锁，完成后控制台无 error/warn。
- N1 浏览器启动短烟测：`http://127.0.0.1:5173/` 启动页 canvas 1280x720，继续调试档进入 `赣鄱 · 窑火瓷镇`，HUD 主轴目标、街景提示和控制台均正常。
- N1 浏览器深烟测：内置浏览器跑通 `?smoke=jiangnan`、`?smoke=bashu`、`?smoke=lingnan`、`?smoke=ganpo`、`?smoke=xiyu`、`?smoke=xiyu-caravan`，五区活动三阶段、收束选择和西域驼队驿站完成态均正常，控制台无 error/warn。
- N2 骨架六区定向守卫：`npm test -- prioritySmokeScenarios priorityMapLayoutEntrypoints priorityScope priorityFrozenIds priorityEndingReport navigationEntrypoints`，7 个测试文件、56 项用例通过。
- N2 骨架六区浏览器短烟测：内置浏览器跑通 `?smoke=qiandian`、`?smoke=jingchu`、`?smoke=huizhou`、`?smoke=jingji`、`?smoke=sanjin`、`?smoke=xueyu`，六区镇务本地活动均可打开并完成，控制台无 error/warn。
- N3 工艺与反馈定向守卫：`npm test -- --run src/engine/__tests__/reducer.test.ts src/engine/__tests__/craftTechniques.test.ts src/data/__tests__/craftInteractions.test.ts`，3 个测试文件、171 项用例通过。
- N3 NPC/开发态 smoke 回归：`npm test -- --run src/dev/__tests__/prioritySmokeScenarios.test.ts src/engine/__tests__/reducer.test.ts`，2 个测试文件、190 项用例通过。
- N3 浏览器短烟测：内置浏览器跑通 `?smoke=xueyu` 的唐卡工坊诊断/最近出品显示，以及 `?smoke=n3-thangka-reject&npc=xy-losang` 的 NPC 拒收可见文本，控制台无 error/warn。
- N4 地图与移动定向守卫：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityScope.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/priorityJourneyNavigation.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，7 个测试文件、60 项用例通过。
- N4 内置浏览器地图 smoke：`?smoke=jiangnan` 中总览地图节点只更新详情不迁移；`?smoke=qiandian`、`?smoke=jingji` HUD/小地图点位正常，控制台无 error/warn。
- N5 交付前压力测试：`npm test -- --run src/engine/__tests__/priorityPreShipStress.test.ts`，6 项用例通过。
- N5 关联回归：`npm test -- --run src/engine/__tests__/priorityPreShipStress.test.ts src/engine/__tests__/priorityJourneyFlow.test.ts src/engine/__tests__/priorityJourney.test.ts src/engine/__tests__/priorityEndingReport.test.ts src/dev/__tests__/prioritySmokeScenarios.test.ts src/data/__tests__/priorityScope.test.ts`，6 个测试文件、54 项用例通过。
- N5 旧夹具同步回归：`npm test -- --run src/engine/__tests__/priorityScopeAudit.test.ts src/game/__tests__/priorityActivityEntrypoints.test.ts src/game/__tests__/priorityJourneyNavigation.test.ts src/game/__tests__/regionSpec.test.ts`，4 个测试文件、31 项用例通过。
- N5 内置浏览器 smoke：`?smoke=jiangnan` 活动可执行；`?smoke=n3-thangka-reject&npc=xy-losang` NPC 拒收可见；`?smoke=xiyu-caravan` 路线活动完成后主轴显示 5/5；控制台无 error。
- M0.9 完整版巡检定向守卫：`npm test -- --run src/engine/__tests__/fullScopeAudit.test.ts src/engine/__tests__/priorityScopeAudit.test.ts`，2 个测试文件、6 项用例通过。
- M0.9 全量回归：`npm test`，25 个测试文件、312 项用例通过。
- M0.9 生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 章节入库定向守卫：`npm test -- --run src/data/__tests__/regionChapters.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/engine/__tests__/regionChapterAudit.test.ts`，3 个测试文件、11 项用例通过。
- M1 章节入库全量回归：`npm test`，28 个测试文件、323 项用例通过。
- M1 章节入库生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 路线落点与章节导航定向守卫：`npm test -- --run src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/engine/__tests__/reducer.test.ts src/game/__tests__/navigationEntrypoints.test.ts src/game/__tests__/regionChapterNavigation.test.ts`，5 个测试文件、199 项用例通过。
- M1 路线落点与章节导航全量回归：`npm test`，29 个测试文件、351 项用例通过。
- M1 路线落点与章节导航类型检查：`npm run typecheck` 通过。
- M1 路线落点与章节导航生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 路线落点与章节导航内置浏览器 smoke：`?smoke=jiangnan` 中 canvas、HUD、小地图、3 个跨区 gate 点和 `秦淮灯市` 镇务入口均可见，控制台无 error/warn。
- M1 独立章节 smoke 定向守卫：`npm test -- --run src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/game/__tests__/regionChapterNavigation.test.ts`，4 个测试文件、81 项用例通过。
- M1 独立章节 smoke 全量回归：`npm test`，30 个测试文件、396 项用例通过。
- M1 独立章节 smoke 类型检查：`npm run typecheck` 通过。
- M1 独立章节 smoke 生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 独立章节 smoke 内置浏览器验证：江南 `chapterSmoke` 入口可完成 `秦淮灯市` 三轮挑战与摊位策略结算；黔滇、西域 `chapterSmoke` 入口均能进入正确地区并暴露唯一可用的章节活动按钮，控制台无 error/warn。截图采集接口本轮超时，未留截图制品。
- M1 真实订单链补线定向守卫：`npm test -- --run src/engine/__tests__/regionChapterOrderChains.test.ts src/data/__tests__/regionChapters.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts`，3 个测试文件、65 项用例通过。
- M1 真实订单链补线类型检查：`npm run typecheck` 通过。
- M1 真实订单链补线全量回归：`npm test`，31 个测试文件、408 项用例通过。
- M1 真实订单链补线生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 真实订单链补线内置浏览器验证：`http://127.0.0.1:5174/?chapterSmoke=chapter-qiandian-silver-tea-road` 可进入黔滇茶马驿道；镇务唯一章节活动 `qd-tea-horse-road` 可打开，新增订单标题 `茶马苗银礼俗回样单` 可见，选择路线挑战后完成体验可结算，控制台无 error/warn。`5173` 已有旧 node 进程监听，本轮新服务自动避让到 `5174`。
- M1 黔滇礼银长线定向守卫：`npm test -- --run src/engine/__tests__/qiandianLongline.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/data/__tests__/priorityFrozenIds.test.ts`，4 个测试文件、27 项用例通过。
- M1 黔滇礼银长线类型检查：`npm run typecheck` 通过。
- M1 黔滇礼银长线全量回归：`npm test`，32 个测试文件、412 项用例通过。
- M1 黔滇礼银长线生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 黔滇礼银长线内置浏览器验证：`http://127.0.0.1:5174/?chapterSmoke=chapter-qiandian-silver-tea-road` 可进入黔滇茶马驿道；镇务活动 `茶马驿道` 可打开，弹窗可见新增订单、`银染茶马会货摊` 和三条摊位策略，选择路线挑战与 `路引来历账` 后可完成体验，控制台无 error/warn。
- M1 京畿声望采办门槛定向守卫：`npm test -- --run src/engine/__tests__/jingjiProcurement.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts`，6 个测试文件、75 项用例通过。
- M1 京畿声望采办门槛类型检查：`npm run typecheck` 通过。
- M1 京畿声望采办门槛全量回归：`npm test`，33 个测试文件、415 项用例通过。
- M1 京畿声望采办门槛生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 京畿声望采办门槛内置浏览器验证：`http://127.0.0.1:5174/?chapterSmoke=chapter-jingji-palace-procurement` 可进入京畿官署门房；镇务活动 `官署门房` 可打开，弹窗可见 `官样景泰蓝采办单`，选择 `先报来路与凭据，再谈条件` 后可完成体验回到街景，控制台无 error/warn。
- M1 雪域供展回访专项守卫：`npm test -- --run src/engine/__tests__/xueyuLongline.test.ts`，1 个测试文件、3 项用例通过。
- M1 雪域供展回访定向守卫：`npm test -- --run src/engine/__tests__/xueyuLongline.test.ts src/engine/__tests__/reducer.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/game/__tests__/regionChapterNavigation.test.ts`，9 个测试文件、260 项用例通过。
- M1 雪域供展回访类型检查：`npm run typecheck` 通过。
- M1 雪域供展回访全量回归：`npm test`，34 个测试文件、418 项用例通过。
- M1 雪域供展回访生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 雪域供展回访内置浏览器验证：`http://127.0.0.1:5174/?chapterSmoke=chapter-xueyu-thangka-snowpass` 可进入雪域高原雪山驿口；镇务活动 `雪山驿口` 可打开，弹窗可见 `雪口唐卡供展补给单`，选择 `按货重、人情与天气重排路线` 后可完成体验回到街景，控制台无 error/warn。
- M1 三晋信用违约回本修复专项守卫：`npm test -- --run src/engine/__tests__/sanjinCreditRecovery.test.ts`，1 个测试文件、3 项用例通过。
- M1 三晋信用违约回本修复定向守卫：`npm test -- --run src/engine/__tests__/sanjinCreditRecovery.test.ts src/engine/__tests__/reducer.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/engine/__tests__/priorityNpcOrderFlow.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/game/__tests__/regionChapterNavigation.test.ts`，10 个测试文件、261 项用例通过。
- M1 三晋信用违约回本修复类型检查：`npm run typecheck` 通过。
- M1 三晋信用违约回本修复全量回归：`npm test`，35 个测试文件、421 项用例通过。
- M1 三晋信用违约回本修复生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 三晋信用违约回本修复内置浏览器验证：`http://127.0.0.1:5173/?chapterSmoke=chapter-sanjin-piaohao-lacquer` 可进入三晋票号古街；镇务活动 `日升票号` 可打开并完成路线挑战；`?npc=sj-lei-zhanggui` 可打开雷掌柜对话并看到信用押货单与信用条款，控制台无 error/warn。
- M1 三晋复违约利钱后日谈专项守卫：`npm test -- --run src/engine/__tests__/sanjinCreditRecovery.test.ts`，1 个测试文件、6 项用例通过。
- M1 三晋复违约利钱后日谈定向守卫：`npm test -- --run src/engine/__tests__/sanjinCreditRecovery.test.ts src/engine/__tests__/reducer.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/engine/__tests__/priorityNpcOrderFlow.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/game/__tests__/regionChapterNavigation.test.ts`，10 个测试文件、264 项用例通过。
- M1 三晋复违约利钱后日谈类型检查：`npm run typecheck` 通过。
- M1 三晋复违约利钱后日谈全量回归：`npm test`，35 个测试文件、424 项用例通过。
- M1 三晋复违约利钱后日谈生产构建：`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1 三晋复违约利钱后日谈内置浏览器记录：按本轮要求只使用 in-app browser；`http://127.0.0.1:5173/?chapterSmoke=chapter-sanjin-piaohao-lacquer&npc=sj-lei-zhanggui` 返回 `net::ERR_BLOCKED_BY_CLIENT`，`http://localhost:5173/...` 被浏览器安全策略拒绝。本轮未改用外部 Edge/Chrome；新增复违约利钱链路由 M1 专项与矩阵测试覆盖。

后续继续执行：

- 原 N5 口径下的美术资源替换与已有人工作街景点位微调仍保留为低优先表现层任务。
- 按 2026-06-14 最新推进口径，程序侧先转入完整版大型化 M0.9，执行依据为 `doc/完整版设计/程序大型化协作指引.md`。

## 下一步程序收束顺序

1. M0.9 全地区生活规格入轨：按固定 5 个团队审计当前代码、数据、地图、工艺、经济与 QA 缺口，形成 11 区程序动作矩阵。
2. M1 全地区核心章节入库：三晋煤铁/醋坊账线、京畿都门市口/漕运复验入口和雪域四张核心人工地图已关闭；继续补雪域颜料矿谷/银器帐房回访、荆楚/徽州长线扩展、多入口章节 smokeBindings 和其他地区剩余人工地图 JSON。
3. 美术替换和地图点位微调不阻塞 M1；只在资源 key 或运行入口需要稳定时同步处理。

## 注意事项

- 总览地图继续只作为调试与信息提示入口。
- 正式游玩迁移仍应通过街景中的通道、城门、码头、驿站、商路节点或交互点触发。
- 主轴目标只负责提示，不直接派发迁移动作。
## 26. M1 三晋煤铁/醋坊账线实化

日期：2026-06-15

- 已实装雷凤池线索后的煤铁保票与醋坊民生日用账双账线。
- 韩账房负责煤铁、重货、账本核验；醋坊郎中负责民生日用、醋坊封签与账本核验。
- 两条线均可通过家访委托完成，也可通过 NPC 订单作为兜底完成，避免玩家因错过家访而卡主线。
- 本轮只推进 M1 闭环；地区移动仍以场景通道/交互点为正式方案，地图点击迁移继续保留为调试用。
- `sanjin-coal-yard` 与 `sanjin-vinegar-yard` 的人工布局 JSON 已由 M1.11 关闭；后续继续压缩其它 M1 直接缺口与 M3 长线经济。
## 27. M1 三晋煤铁/醋坊人工地图

日期：2026-06-15

- 已新增 `sanjin-coal-yard` 与 `sanjin-vinegar-yard` 两张人工地图 JSON。
- 煤铁窑塬落入采煤、采铁、冶铁、煤铁活动、韩账房和区内通道。
- 清徐醋坊落入老陈醋工艺、醋坊活动、醋郎中和区内通道。
- 本轮只做程序可达与入口稳定，最终美术资源、瓦片替换和精细摆位后置。
- 三晋 M1 直接缺口从“账线 + 人工地图”收束为“M3 长线经济和后续内容密度”。
- M1.11 验证：`npm test` 35 个测试文件、430 项用例通过；`npm run typecheck` 与 `npm run build` 通过；内置浏览器从票号古街实际经区内 gate 进入煤铁窑塬，再经煤铁窑塬进入清徐醋坊，并打开清徐醋坊活动弹窗，控制台无 error。
## 28. M1 京畿都门市口与漕运复验入口

日期：2026-06-15

- 已新增 `jingji-market-gate` 人工地图 JSON，并注册进运行时地图管线。
- 都门市口落入 `inner-painting` 工艺、`jj-appraisal-market` 活动、`jj-meng-zhangyan` NPC 和通往宫造大院 / 官署门房 / 江南 / 三晋的街景 gate。
- `jj-appraisal-market` 已新增市口背书订单 `都门市口官样背书单`，读取景泰蓝作品并绑定漕运与官路路线。
- 宋押司 palace 采办条款已读取漕运料账清账/滞账旗标，形成清账加信、滞账降信的复验分支。
- 本轮仍只做程序闭环与入口稳定，不处理最终美术替换和精细摆位。
- M1.12 聚焦验证：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/jingjiProcurement.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，9 个测试文件、80 项用例通过。
- M1.12 全量验证：`npm test` 35 个测试文件、434 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1.12 内置浏览器 smoke：`?chapterSmoke=chapter-jingji-palace-procurement&qaSeason=autumn&qaWeather=clear` 从官署门房经街景 gate 到都门市口，打开并完成 `都门市口` 活动后再经街景 gate 返回官署门房，控制台 error/warn 为空。
## 29. M1 雪域雪山驿口人工地图

日期：2026-06-15

- 已新增 `xueyu-snow-pass` 人工地图 JSON，并注册进运行时地图管线。
- 雪山驿口落入 `xy-snow-pass` 活动、`xy-yak-captain` NPC、通往唐卡画院 / 颜料矿谷 / 银器帐房的区内 gate，以及通往巴蜀 / 西域的跨区 gate。
- 本轮只做程序可达与入口稳定，不扩雪山驿路护商后日谈，不处理最终雪地美术和精细摆位。
- 雪域 M1 直接地图缺口从“雪山驿口 / 颜料矿谷 / 银器帐房”收束为“颜料矿谷 / 银器帐房”。
- M1.13 聚焦验证：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/data/__tests__/regionChapters.test.ts src/engine/__tests__/xueyuLongline.test.ts src/engine/__tests__/regionChapterOrderChains.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，9 个测试文件、80 项用例通过。
- M1.13 全量验证：`npm test` 35 个测试文件、435 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1.13 内置浏览器 smoke：`?chapterSmoke=chapter-xueyu-thangka-snowpass&qaSeason=winter&qaWeather=snow` 进入雪域雪山驿口；可打开并完成 `雪山驿口` 活动；经街景区内 gate 到唐卡画院并返回；经街景跨区 gate 往返巴蜀茶马驿与西域驼队驿站；全程未使用总览地图迁移，控制台 error/warn 为空。

## 30. M1 雪域颜料矿谷与银器帐房人工地图

日期：2026-06-15

- 已新增 `xueyu-pigment-valley` 与 `xueyu-silver-tent` 两张人工地图 JSON，并注册进运行时地图管线。
- 颜料矿谷落入 `harvest-pigment`、`grind-pigment`、`xy-pigment-valley` 与 `xy-shicai-tong`，并通过街景 gate 连接雪山驿口、唐卡画院、银器帐房、巴蜀与西域。
- 银器帐房落入 `harvest-silver-ore`、`refine-silver`、`tibetan-silver`、`xy-silver-tent` 与 `xy-baiyinshu`，并通过街景 gate 回接雪山驿口、唐卡画院、颜料矿谷、巴蜀与西域。
- 为 `tibetan-silver` 补最小三阶段工艺交互，为 `xy-silver-tent` 补活动挑战，避免新地图入口只有点位而没有可执行玩法钩子。
- 雪域 M1 直接人工地图缺口从“雪山驿口 / 颜料矿谷 / 银器帐房”收束为“无直接核心地图缺口”；雪域仍保持 `needs-expansion`，后续继续补颜料矿谷/银器帐房人物回访、材料状态反馈、雪山驿路护商后日谈和多入口章节 smokeBindings。
- M1.14 聚焦验证已通过：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/craftInteractions.test.ts src/engine/__tests__/craftTechniques.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/data/__tests__/regionChapters.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，9 个测试文件、78 项用例通过。
- M1.14 DEV 起点守卫已通过：`npm test -- --run src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapters.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，4 个测试文件、71 项用例通过。
- M1.14 全量验证已通过：`npm test` 35 个测试文件、439 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1.14 内置浏览器 smoke 已通过：使用 DEV-only `smokeSubregion` 分别进入颜料矿谷与银器帐房，完成 `xy-pigment-valley` / `xy-silver-tent` 活动，并从银器帐房镇务面板打开 `tibetan-silver` 工艺页与 `craft-feedback:tibetan-silver`；控制台 error/warn 为空，截图接口在内置浏览器后端超时未产出图片。
- `smokeSubregion` 只用于 DEV 浏览器 QA，同地区校验由测试守住；正式移动仍必须经街景 gate / 区内通道触发，不使用总览地图迁移作为正式可达性证明。

## 31. M1 西域艾德莱斯织坊人工地图与工艺交互

日期：2026-06-15

- 已新增 `xiyu-atlas-loom` 人工地图 JSON，并注册进运行时地图管线。
- 艾德莱斯织坊落入 `harvest-cocoon`、`sericulture`、`atlas-silk`、`xiyu-atlas-loom` 与 `xu-guli`，并通过街景 gate 连接绿洲巴扎、昆仑玉场、驼队驿站和雪域跨区路。
- 驼队驿站补到织坊的区内 gate，西域四个核心小地区形成街景互通；跨区路线仍通过 `route-xueyu-xiyu-caravan` 落到 `xiyu-caravan-post`，不把织坊改成路线默认落点。
- 已为 `atlas-silk` 补三阶段工艺交互、缺陷、返修和订单钩子，并为 `xiyu-atlas-loom` 补活动挑战 `xiyu-atlas-loom-tie-dye`。
- 西域 M1 直接缺口从“艾德莱斯织坊缺人工 JSON 与专属工艺交互”收束为“巴扎长线商路风险、织坊藏客回访与订单差异”；地毯和铜器专属工艺规格仍留待后续，不在本轮扩大范围。
- M1.15 聚焦验证已通过：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/data/__tests__/craftInteractions.test.ts src/engine/__tests__/craftTechniques.test.ts src/engine/__tests__/fullScopeAudit.test.ts src/dev/__tests__/regionChapterSmokeScenarios.test.ts src/data/__tests__/regionChapters.test.ts src/data/__tests__/regionChapterMapEntrypoints.test.ts src/game/__tests__/regionSpec.test.ts src/game/__tests__/navigationEntrypoints.test.ts`，11 个测试文件、132 项用例通过。
- M1.15 全量验证已通过：`npm test` 35 个测试文件、443 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1.15 内置浏览器 smoke 已通过：`?chapterSmoke=chapter-xiyu-bazaar-caravan&smokeSubregion=xiyu-atlas-loom&qaSeason=autumn&qaWeather=clear` 进入织坊，完成 `xiyu-atlas-loom` 活动，打开并制作 `atlas-silk`，可见 `craft-feedback:atlas-silk` 与 `craft-recent:atlas-silk`；控制台 error/warn 为空。内置浏览器对 Phaser canvas 的坐标/键盘移动没有稳定触发角色走到 gate，正式 gate 可达性已由测试守卫覆盖，不使用总览地图迁移替代。
- 本轮只冻结程序 ID、交互目标和数据链路，不代表最终织坊美术、织机装饰、NPC 站姿或精细摆位完成。
## 32. M1 赣鄱高岭矿丘与河运柴场人工地图
日期：2026-06-15

- 已新增 `ganpo-kaolin-hill` 与 `ganpo-river-wood` 两张人工地图 JSON，并注册进运行时地图管线。
- 高岭矿丘落入 `harvest-kaolin`、`mine-kaolin`、`gp-kaolin-hill`、`gp-shi-bai` 与通往窑镇/河运柴场/江南/徽州/荆楚的 gate。
- 河运柴场落入 `harvest-coal`、`xiabu`、`gp-river-wood-yard`、`gp-chai-yazi` 与通往窑镇/高岭矿丘/江南/徽州/荆楚的 gate。
- 赣鄱 M1 直接人工地图缺口已由 M1.16 收束；剩余优先缺口转向 `xiabu` 专属工艺交互、瓷土/釉料来源读数、柴运长线风险与多入口 smokeBindings。
- 正式移动仍以街景 gate / 区内通道触发为准；`smokeSubregion` 仅用于 DEV 浏览器 QA 起点，不作为正式迁移方式。
- M1.16 聚焦验证已通过：`npm test -- --run src/data/__tests__/mapLayout.test.ts src/data/__tests__/priorityMapLayoutEntrypoints.test.ts src/data/__tests__/priorityFrozenIds.test.ts src/engine/__tests__/regionChapterAudit.test.ts src/engine/__tests__/fullScopeAudit.test.ts`，5 个测试文件、49 项用例通过。
- M1.16 全量验证已通过：`npm test` 35 个测试文件、445 项用例通过；`npm run typecheck` 通过；`npm run build` 通过，保留既有 Vite 大 chunk 提醒。
- M1.16 内置浏览器 smoke 已通过：两个 DEV-only 起点分别进入高岭矿丘与河运柴场；高岭矿丘活动可打开并完成；河运柴场挑战选择“按货重、人情与天气重排路线”后可完成；控制台 error/warn 为空。正式 gate 可达性由地图/导航守卫覆盖，不使用总览地图迁移替代。
